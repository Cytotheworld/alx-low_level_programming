#include <stdio.h>
/**
 * main - prints in lower case
 * Return: 0 (success)
 */

int main(void)
{
	char 'a';

	c = 'a';
	while
		(c <= 'z') {
			if ((c != 'q' && c != 'e') && c <= 'z')
				putchar(c);
		}
	putchar('\n');
	return (0);
}
